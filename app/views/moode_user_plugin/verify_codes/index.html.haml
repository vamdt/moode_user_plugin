%h3 没有使用的验证码

%table.table
  %tr
    %th Code
    %th
    %th
    %th

  - @verify_codes.each do |verify_code|
    %tr
      %td= verify_code.code
      %td
        = link_to '作废', verify_code, method: :delete, data: { confirm: 'Are you sure?' }

%br

= form_tag "/admin/verify_codes/batch" do
  .field
    = label_tag :vcode_number, "生成验证码"
    = number_field_tag :vcode_number, 1, :min => 1
  .actions
    = submit_tag 'Save'

